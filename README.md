# 机器人资产管理系统

> 🎯 **新手必读**：
> - 🚀 [快速入门](快速入门.md) - 3分钟极速上手
> - 📖 [小白操作指南](小白操作指南.md) - 零基础详细教程
> - 🔄 [操作流程图](操作流程图.md) - 可视化操作步骤
> - ❓ [常见问题FAQ](FAQ常见问题.md) - 问题解决方案
> 
> 🛠️ **环境配置工具**：
> - 📖 [环境配置说明.md](环境配置说明.md) - 详细使用指南
> - 🐍 [conda配置指南.md](conda配置指南.md) - Conda虚拟环境配置
> - 🔄 [one_click_setup.bat](one_click_setup.bat) - Graphical setup wizard
> - ⚡ [快速配置.bat](快速配置.bat) - 命令行快速配置
> - 🛠️ [修复版配置.bat](修复版配置.bat) - 增强错误处理版本
> - 🆘 [应急配置.bat](应急配置.bat) - 简化版配置工具
> - 🛡️ [防卡死配置.bat](防卡死配置.bat) - 稳定性增强版本
> - 🔍 [诊断工具.bat](诊断工具.bat) - 精确诊断工具
> 
> 🚀 **最快启动**：双击项目根目录下的 `test.bat` 文件自动检测环境！

## 项目简介

机器人资产管理系统是一套完整的企业级资产管理解决方案，专门用于管理各类机器人设备的全生命周期。系统采用现代化的技术架构，提供从资产采购、入库、领用、调拨、维修保养到报废退役的全流程管理功能。

## 技术架构

### 后端技术栈
- **Spring Boot 3** - 核心框架
- **MyBatis-Plus** - ORM框架
- **Spring Security + JWT** - 权限认证
- **MySQL 8.0** - 主数据库
- **Redis** - 缓存和会话存储
- **Maven** - 项目构建工具

### 前端技术栈
- **Vue 3** - 前端框架
- **Element Plus** - UI组件库
- **Pinia** - 状态管理
- **Vue Router** - 路由管理
- **Axios** - HTTP客户端
- **ECharts** - 数据可视化

### 部署技术
- **Docker** - 容器化部署
- **Nginx** - 反向代理和负载均衡
- **Docker Compose** - 多容器编排

## 功能特性

### 📦 资产台账管理
- 机器人信息CRUD操作
- 支持多种机器人类型（工业机器人、协作机器人、AGV、服务机器人等）
- 资产状态跟踪（在库、已领用、维修中、已报废、已调拨）
- 批量导入导出功能
- 二维码/条形码生成

### 📥 入库管理
- 多种入库类型（新购入库、归还入库、维修回库、调拨入库）
- 入库单管理与审批流程
- 自动更新资产状态

### 📤 出库管理
- 多种出库类型（领用出库、调拨出库、维修送出、报废出库）
- 出库审批流程
- 责任人和部门管理

### 🔄 调拨管理
- 跨部门/跨仓库调拨
- 双方确认流程
- 调拨历史记录

### 🔧 维修保养管理
- 维修工单管理
- 定期保养计划
- 故障记录和维修历史

### 🗑️ 报废管理
- 报废申请和审批
- 残值评估
- 报废资产状态管理

### 📊 数据统计与看板
- 资产总览仪表板
- 出入库趋势分析
- 部门资产分布统计
- 预警提醒功能

## 使用方法

### 🛠️ 环境准备

#### 系统要求
- **操作系统**：Windows 10+/Linux/macOS
- **Java环境**：JDK 17 或更高版本
- **前端环境**：Node.js 16+ 和 npm 8+
- **数据库**：MySQL 8.0+
- **缓存服务**：Redis 6.0+（可选）
- **容器化**：Docker & Docker Compose（推荐用于生产环境）

### 🚀 快速部署

#### 方法一：本地开发部署

**1. 克隆项目并进入目录**
```bash
git clone <项目地址>
cd robot-asset-management
```

**2. 数据库初始化**
```bash
# 登录MySQL
mysql -u root -p

# 执行初始化脚本
source sql/init.sql
```
或者直接在MySQL客户端中执行 `sql/init.sql` 文件

**3. 配置后端环境**
```bash
# 修改数据库连接配置
vim src/main/resources/application.yml

# 主要配置项：
# spring.datasource.url: jdbc:mysql://localhost:3306/robot_asset
# spring.datasource.username: root
# spring.datasource.password: your_password
```

**4. 启动后端服务**
```bash
# 编译项目
mvn clean package

# 运行应用
mvn spring-boot:run

# 或者直接运行jar包
java -jar target/robot-asset-management-1.0.0.jar
```

**5. 启动前端服务**
```bash
# 安装依赖
npm install

# 启动开发服务器
npm run serve
```

**6. 访问系统**
- 后端API：http://localhost:8080
- 前端界面：http://localhost:8081
- API文档：http://localhost:8080/swagger-ui.html

#### 方法二：Docker一键部署

**1. 确保Docker环境**
```bash
docker --version
docker-compose --version
```

**2. 一键部署**
```bash
# 构建并启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看实时日志
docker-compose logs -f
```

**3. 系统访问**
- Web界面：http://localhost
- 后端API：http://localhost:8080
- 数据库：localhost:3306
- Redis：localhost:6379

### 🔧 系统配置详解

#### 数据库配置
```yaml
# application.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/robot_asset?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: your_password
  redis:
    host: localhost
    port: 6379
```

#### 系统参数配置
```yaml
# 资产编号前缀
system:
  asset-no-prefix: RA
  
# JWT配置
jwt:
  secret: your_secret_key
  expiration: 86400000  # 24小时
```

### 💻 开发指南

#### 后端开发

**1. 项目结构说明**
```
src/main/java/com/company/robot/
├── RobotAssetApplication.java    # 启动类
├── config/                       # 配置类
├── controller/                   # 控制器层
├── service/                      # 服务层
├── mapper/                       # 数据访问层
├── entity/                       # 实体类
├── vo/                           # 视图对象
└── common/                       # 公共组件
```

**2. 添加新的业务模块**
```java
// 1. 创建实体类
@Entity
@Table(name = "your_table")
public class YourEntity {}

// 2. 创建Mapper接口
@Mapper
public interface YourMapper extends BaseMapper<YourEntity> {}

// 3. 创建Service接口和实现
public interface YourService extends IService<YourEntity> {}

@Service
public class YourServiceImpl extends ServiceImpl<YourMapper, YourEntity> implements YourService {}

// 4. 创建Controller
@RestController
@RequestMapping("/api/your")
public class YourController {}
```

#### 前端开发

**1. 项目结构说明**
```
src/
├── main.js              # 入口文件
├── router/              # 路由配置
├── store/               # 状态管理
├── views/               # 页面组件
├── components/          # 公共组件
└── utils/               # 工具函数
```

**2. 添加新的页面**
```javascript
// 1. 创建页面组件
// views/your/NewPage.vue

// 2. 配置路由
// router/index.js
{
  path: '/your/page',
  component: () => import('@/views/your/NewPage.vue')
}

// 3. 调用API
import request from '@/utils/request'

request({
  url: '/api/your/data',
  method: 'get'
})
```

### 🔍 功能使用指南

#### 1. 资产管理
- **新增资产**：点击"新增资产"按钮，填写资产信息
- **资产查询**：支持按编号、名称、类型等条件搜索
- **资产编辑**：点击表格中的"编辑"按钮修改资产信息
- **资产删除**：支持单个删除和批量删除
- **状态变更**：资产状态会根据业务流程自动更新

#### 2. 入库管理
- **新建入库单**：选择入库类型，填写相关信息
- **入库审批**：提交后需要相应权限人员审批
- **资产上架**：审批通过后资产自动更新为"在库"状态

#### 3. 出库管理
- **申请出库**：填写出库申请单
- **审批流程**：经过部门主管和资产管理员审批
- **资产下架**：出库完成后资产状态更新

#### 4. 调拨管理
- **发起调拨**：填写调拨申请
- **双方确认**：调出方和调入方都需要确认
- **资产转移**：确认后资产归属部门变更

#### 5. 维修保养
- **故障报修**：填写维修申请单
- **维修跟踪**：记录维修过程和费用
- **保养计划**：设置定期保养提醒

### 🛡️ 权限管理

#### 角色说明
- **超级管理员**：拥有所有权限
- **资产管理员**：负责资产管理相关操作
- **部门主管**：管理本部门资产和审批
- **普通用户**：只能查看和申请相关操作

#### 权限配置
```sql
# 在sys_permission表中添加新权限
INSERT INTO sys_permission (permission_name, permission_code, menu_path) 
VALUES ('新功能', 'new:function', '/new/path');

# 分配给角色
INSERT INTO sys_role_permission (role_id, permission_id) 
VALUES (role_id, permission_id);
```

### 📊 数据备份与恢复

#### 数据库备份
```bash
# 备份整个数据库
mysqldump -u root -p robot_asset > backup.sql

# 备份特定表
docker exec robot-mysql mysqldump -u root -proot123 robot_asset robot_asset > asset_backup.sql
```

#### 数据恢复
```bash
# 恢复数据库
mysql -u root -p robot_asset < backup.sql
```

#### Docker数据卷管理
```bash
# 查看数据卷
docker volume ls

# 备份数据卷
docker run --rm -v robot_asset_mysql_data:/data -v $(pwd):/backup alpine tar czf /backup/mysql_backup.tar.gz -C /data .
```

### 🔧 常见问题解决

#### 启动问题
```bash
# 检查端口占用
netstat -an | grep 8080

# 清理缓存重新编译
mvn clean compile

# 重启Docker服务
docker-compose down
docker-compose up -d
```

#### 数据库连接问题
```bash
# 测试数据库连接
mysql -h localhost -u root -p -P 3306

# 检查Docker中的MySQL
docker exec -it robot-mysql mysql -u root -proot123
```

#### 前端开发问题
```bash
# 清理node_modules
rm -rf node_modules package-lock.json
npm install

# 检查代理配置
# vue.config.js 中的 devServer.proxy 配置
```

### 📈 性能优化建议

#### 数据库优化
- 定期执行 `ANALYZE TABLE` 和 `OPTIMIZE TABLE`
- 合理设置索引，避免全表扫描
- 监控慢查询日志

#### 应用优化
- 启用Redis缓存热点数据
- 配置合理的线程池大小
- 使用连接池管理数据库连接

#### 前端优化
- 启用Gzip压缩
- 使用CDN加速静态资源
- 实施懒加载和代码分割

## API文档
系统集成Swagger API文档，启动后访问：http://localhost:8080/swagger-ui.html

## 默认账号
- **用户名**：admin
- **密码**：admin123
- **角色**：超级管理员

> ⚠️ **安全提醒**：生产环境中请务必修改默认密码！

## 项目结构
```
robot-asset-management/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/company/robot/
│   │   │       ├── RobotAssetApplication.java
│   │   │       ├── config/          # 配置类
│   │   │       ├── controller/      # 控制器
│   │   │       ├── service/         # 服务层
│   │   │       ├── mapper/          # 数据访问层
│   │   │       ├── entity/          # 实体类
│   │   │       ├── vo/              # 视图对象
│   │   │       └── common/          # 公共组件
│   │   └── resources/
│   │       ├── application.yml      # 配置文件
│   │       └── mapper/              # MyBatis映射文件
├── frontend/                        # 前端项目
├── docker-compose.yml              # Docker编排文件
├── Dockerfile                      # 后端Dockerfile
└── frontend.Dockerfile             # 前端Dockerfile
```

## 许可证
MIT License