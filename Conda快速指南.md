# 🐍 Conda环境使用快速指南

## 🚀 三步完成配置

### 第一步：安装Conda
```bash
# 下载并安装Miniconda（推荐）
# 访问：https://docs.conda.io/en/latest/miniconda.html
```

### 第二步：创建项目环境
```bash
# 双击运行
conda配置.bat
```

### 第三步：激活环境并启动
```bash
# 激活环境
conda activate robot-asset

# 启动后端
mvn spring-boot:run

# 启动前端（新终端）
npm run serve
```

## 🎯 Conda环境优势

✅ **环境隔离** - 不影响系统其他Java/Node.js项目  
✅ **版本管理** - 自动处理依赖版本冲突  
✅ **一键安装** - 所有工具一次性配置完成  
✅ **跨平台** - Windows/Mac/Linux通用  
✅ **易于分享** - environment.yml文件可共享环境配置  

## 🛠️ 常用Conda命令

```bash
# 查看环境列表
conda env list

# 激活环境
conda activate robot-asset

# 退出环境
conda deactivate

# 删除环境
conda env remove -n robot-asset

# 更新环境
conda env update -f environment.yml
```

## ⚡ 快速切换开发模式

```bash
# 开发时激活环境
conda activate robot-asset

# 开发完成后退出
conda deactivate

# 需要时重新激活即可继续开发
```

## 🆘 常见问题

**Q: Conda环境占用空间大吗？**
A: 约2-3GB，但避免了版本冲突问题

**Q: 可以和其他项目共用吗？**
A: 建议每个项目独立环境，避免依赖冲突

**Q: 环境损坏了怎么办？**
A: 直接删除重建：`conda env remove -n robot-asset` 然后重新运行配置脚本

---
💡 **推荐指数**：⭐⭐⭐⭐⭐ 适合开发者和需要环境隔离的用户